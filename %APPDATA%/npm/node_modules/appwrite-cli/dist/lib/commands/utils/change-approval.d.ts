/**
 * Check if a value is considered empty
 */
export declare const isEmpty: (value: any) => boolean;
/**
 * Prompt user for confirmation to proceed with push
 */
export declare const getConfirmation: () => Promise<boolean>;
/**
 * Compare two objects and return their differences
 */
interface ObjectChange {
    group: string;
    setting: string;
    remote: string;
    local: string;
}
export declare const getObjectChanges: <T extends Record<string, any>>(remote: T, local: T, index: keyof T, what: string) => ObjectChange[];
/**
 * Approve changes before pushing resources
 * Compares local resources with remote resources and prompts user for confirmation
 */
export declare const approveChanges: (resource: any[], resourceGetFunction: Function, keys: Set<string>, resourceName: string, resourcePlural: string, skipKeys?: string[], secondId?: string, secondResourceName?: string) => Promise<boolean>;
export {};
//# sourceMappingURL=change-approval.d.ts.map